import { JustifyHeaderFooterDirective } from './justify-header-footer.directive';

describe('JustifyHeaderFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new JustifyHeaderFooterDirective();
    expect(directive).toBeTruthy();
  });
});
