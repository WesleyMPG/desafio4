import { CloseBtnStyleDirective } from './close-btn-style.directive';

describe('CloseBtnStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseBtnStyleDirective();
    expect(directive).toBeTruthy();
  });
});
