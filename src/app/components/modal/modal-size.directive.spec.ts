import { ModalSizeDirective } from './modal-size.directive';

describe('ModalSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalSizeDirective();
    expect(directive).toBeTruthy();
  });
});
